// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/novro-gateway/novro/ent/user"
	"github.com/novro-gateway/novro/ent/wallet"
	"github.com/novro-gateway/novro/ent/walletentry"
)

// WalletEntry is the model entity for the WalletEntry schema.
type WalletEntry struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// WalletID holds the value of the "wallet_id" field.
	WalletID uuid.UUID `json:"wallet_id,omitempty"`
	// ActorUserID holds the value of the "actor_user_id" field.
	ActorUserID *uuid.UUID `json:"actor_user_id,omitempty"`
	// ReferenceID holds the value of the "reference_id" field.
	ReferenceID uuid.UUID `json:"reference_id,omitempty"`
	// EntryType holds the value of the "entry_type" field.
	EntryType walletentry.EntryType `json:"entry_type,omitempty"`
	// AmountMicros holds the value of the "amount_micros" field.
	AmountMicros int64 `json:"amount_micros,omitempty"`
	// BalanceAfterMicros holds the value of the "balance_after_micros" field.
	BalanceAfterMicros int64 `json:"balance_after_micros,omitempty"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the WalletEntryQuery when eager-loading is set.
	Edges        WalletEntryEdges `json:"edges"`
	selectValues sql.SelectValues
}

// WalletEntryEdges holds the relations/edges for other nodes in the graph.
type WalletEntryEdges struct {
	// Wallet holds the value of the wallet edge.
	Wallet *Wallet `json:"wallet,omitempty"`
	// Actor holds the value of the actor edge.
	Actor *User `json:"actor,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// WalletOrErr returns the Wallet value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e WalletEntryEdges) WalletOrErr() (*Wallet, error) {
	if e.Wallet != nil {
		return e.Wallet, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: wallet.Label}
	}
	return nil, &NotLoadedError{edge: "wallet"}
}

// ActorOrErr returns the Actor value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e WalletEntryEdges) ActorOrErr() (*User, error) {
	if e.Actor != nil {
		return e.Actor, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "actor"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*WalletEntry) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case walletentry.FieldActorUserID:
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case walletentry.FieldAmountMicros, walletentry.FieldBalanceAfterMicros:
			values[i] = new(sql.NullInt64)
		case walletentry.FieldEntryType, walletentry.FieldDescription:
			values[i] = new(sql.NullString)
		case walletentry.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case walletentry.FieldID, walletentry.FieldWalletID, walletentry.FieldReferenceID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the WalletEntry fields.
func (_m *WalletEntry) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case walletentry.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case walletentry.FieldWalletID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field wallet_id", values[i])
			} else if value != nil {
				_m.WalletID = *value
			}
		case walletentry.FieldActorUserID:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field actor_user_id", values[i])
			} else if value.Valid {
				_m.ActorUserID = new(uuid.UUID)
				*_m.ActorUserID = *value.S.(*uuid.UUID)
			}
		case walletentry.FieldReferenceID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field reference_id", values[i])
			} else if value != nil {
				_m.ReferenceID = *value
			}
		case walletentry.FieldEntryType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field entry_type", values[i])
			} else if value.Valid {
				_m.EntryType = walletentry.EntryType(value.String)
			}
		case walletentry.FieldAmountMicros:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field amount_micros", values[i])
			} else if value.Valid {
				_m.AmountMicros = value.Int64
			}
		case walletentry.FieldBalanceAfterMicros:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field balance_after_micros", values[i])
			} else if value.Valid {
				_m.BalanceAfterMicros = value.Int64
			}
		case walletentry.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				_m.Description = value.String
			}
		case walletentry.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the WalletEntry.
// This includes values selected through modifiers, order, etc.
func (_m *WalletEntry) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryWallet queries the "wallet" edge of the WalletEntry entity.
func (_m *WalletEntry) QueryWallet() *WalletQuery {
	return NewWalletEntryClient(_m.config).QueryWallet(_m)
}

// QueryActor queries the "actor" edge of the WalletEntry entity.
func (_m *WalletEntry) QueryActor() *UserQuery {
	return NewWalletEntryClient(_m.config).QueryActor(_m)
}

// Update returns a builder for updating this WalletEntry.
// Note that you need to call WalletEntry.Unwrap() before calling this method if this WalletEntry
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *WalletEntry) Update() *WalletEntryUpdateOne {
	return NewWalletEntryClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the WalletEntry entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *WalletEntry) Unwrap() *WalletEntry {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: WalletEntry is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *WalletEntry) String() string {
	var builder strings.Builder
	builder.WriteString("WalletEntry(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("wallet_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.WalletID))
	builder.WriteString(", ")
	if v := _m.ActorUserID; v != nil {
		builder.WriteString("actor_user_id=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("reference_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ReferenceID))
	builder.WriteString(", ")
	builder.WriteString("entry_type=")
	builder.WriteString(fmt.Sprintf("%v", _m.EntryType))
	builder.WriteString(", ")
	builder.WriteString("amount_micros=")
	builder.WriteString(fmt.Sprintf("%v", _m.AmountMicros))
	builder.WriteString(", ")
	builder.WriteString("balance_after_micros=")
	builder.WriteString(fmt.Sprintf("%v", _m.BalanceAfterMicros))
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(_m.Description)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// WalletEntries is a parsable slice of WalletEntry.
type WalletEntries []*WalletEntry
